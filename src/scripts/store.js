/**This document will handle the manipulation and store of the
 * data for our app
 */

const bookmarks = [];
let addingBookmark = false;
let error = null;
let filter = 0;



export default {
  bookmarks,
  addingBookmark,
  error,
  filter
}